---
export function getStaticPaths() {
	return [
		{ params: { lang: "en", slug: "test" }},
		{ params: { lang: "tr", slug: "test" }},
	];
}

const { lang, slug } = Astro.params;

---

<p>this is page in {JSON.stringify(Astro.params)}</p>